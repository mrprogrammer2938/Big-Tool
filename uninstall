echo -n "uninstalling Big-Tool ";
sleep 3 & while [ "$(ps a | awk '{print $1}' | grep $!)" ] ; do for X in '-' '\' '|' '/'; do echo -en "\b$X"; sleep 0.1; done; done
cd .. && sudo rm -r Big-Tool
sleep 2
echo "finish!"
exit 1
